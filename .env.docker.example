# 이 파일을 .env.docker로 복사한 뒤 예시 값을 교체하고 docker compose를 실행한다.
# .env.docker는 커밋하지 않는다.
# 어드민 API의 ADMIN_ASSET_S3_ACCESS_KEY, ADMIN_ASSET_S3_SECRET_KEY 값은
# 아래 RustFS credential과 일치해야 한다.
RUSTFS_ACCESS_KEY=replace-with-local-rustfs-access-key
RUSTFS_SECRET_KEY=replace-with-local-rustfs-secret-key
