LEARNER_AUTH_SECRET=replace-with-32-byte-local-learner-secret
ADMIN_AUTH_SECRET=replace-with-distinct-32-byte-admin-secret
CURSOR_SIGNING_SECRET=replace-with-distinct-32-byte-cursor-secret
ADMIN_ORIGIN=http://localhost:3001
DATABASE_URL=file:data/api.sqlite
NODE_ENV=development
DEPLOYMENT_ENVIRONMENT=development
ENABLE_API_DOCS=true
DEPLOYMENT_VERSION=local
API_PORT=4000
WEB_ORIGIN=http://localhost:3000
ADMIN_SEED_EMAIL=owner@example.com
ADMIN_SEED_NAME=관리자
ADMIN_SEED_PASSWORD=replace-with-strong-local-admin-password
ADMIN_SEED_RESET_PASSWORD=true
# 운영 seed에만 아래 두 값을 명시하고 DATABASE_URL과 정확히 일치시킨다.
# ADMIN_SEED_PRODUCTION_APPROVED=true
# ADMIN_SEED_EXPECTED_DATABASE_URL=file:/absolute/path/to/api.sqlite
# 운영 기준 콘텐츠 seed에만 아래 두 값을 명시하고 DATABASE_URL과 정확히 일치시킨다.
# DATABASE_SEED_PRODUCTION_APPROVED=true
# DATABASE_SEED_EXPECTED_DATABASE_URL=file:/absolute/path/to/api.sqlite
# 로컬 기본 학습은 bundled thumbnail을 사용한다. 업로드를 검증할 때만 아래 묶음을 모두 설정한다.
# ADMIN_ASSET_S3_ACCESS_KEY=replace-with-r2-access-key
# ADMIN_ASSET_S3_SECRET_KEY=replace-with-r2-secret-key
# ADMIN_ASSET_S3_BUCKET=writing-app-assets
# ADMIN_ASSET_S3_ENDPOINT=https://<account-id>.r2.cloudflarestorage.com
# ADMIN_ASSET_S3_REGION=auto
# ADMIN_ASSET_PUBLIC_BASE_URL=https://assets.example.com
# DELETION_MARKER_S3_ACCESS_KEY=replace-with-private-s3-access-key
# DELETION_MARKER_S3_SECRET_KEY=replace-with-private-s3-secret-key
# DELETION_MARKER_S3_BUCKET=writing-app-deletion-markers
# DELETION_MARKER_S3_ENDPOINT=https://<account-id>.r2.cloudflarestorage.com
# DELETION_MARKER_S3_REGION=auto
# DELETION_MARKER_S3_PREFIX=privacy/deletion-markers
# 삭제 보존 기간은 제품 요구사항이 소유한 정책값이며 미지정 시 identity 기본값을 쓴다.
# 바꾸려면 개인정보·요구사항 문서를 같은 변경에서 갱신한다.
# LEARNER_DELETION_RETENTION_DAYS=5
# OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-5.2
# 일일 quota 수치는 제품 entitlement가 아닌 출시 검증 전 운영 safeguard 예시다.
# AI_FEEDBACK_USER_DAILY_REQUEST_LIMIT=20
# AI_FEEDBACK_USER_DAILY_SUCCESS_LIMIT=10
# AI_FEEDBACK_GLOBAL_DAILY_REQUEST_LIMIT=1000
# AI_FEEDBACK_GLOBAL_DAILY_SUCCESS_LIMIT=500
# AI_FEEDBACK_PROVIDER_TIMEOUT_MS=30000
# AI_FEEDBACK_PENDING_TTL_MS=60000
# GOOGLE_CLIENT_ID=replace-with-google-client-id
# GOOGLE_CLIENT_SECRET=replace-with-google-client-secret
# RESEND_API_KEY=re_...
# AUTH_EMAIL_FROM=글결 <auth@example.com>
# AUTH_EMAIL_REPLY_TO=support@example.com
# 관리자 MCP는 승인된 OAuth provider와 외부 데이터 처리 경계가 확정된 경우에만 묶음 전체를 설정한다.
# ADMIN_MCP_ENABLED=true
# ADMIN_MCP_RESOURCE_URL=http://localhost:4000/mcp/admin
# ADMIN_MCP_OAUTH_ISSUER=http://localhost:9000
# ADMIN_MCP_OAUTH_METADATA_URL=http://localhost:9000/.well-known/oauth-authorization-server
# ADMIN_MCP_INTROSPECTION_CLIENT_ID=replace-with-resource-server-client-id
# ADMIN_MCP_INTROSPECTION_CLIENT_SECRET=replace-with-resource-server-client-secret
# ADMIN_MCP_OWNER_SUBJECT=replace-with-approved-owner-subject
# ADMIN_MCP_OWNER_ADMIN_ID=replace-with-existing-owner-admin-id
# 외부 OAuth 범위와 운영 승인 절차를 확인하기 전에는 변경 기능을 활성화하지 않습니다.
# ADMIN_MCP_CHANGES_ENABLED=false
# ADMIN_MCP_APPROVAL_TTL_SECONDS=300
# ADMIN_MCP_EXECUTION_LEASE_SECONDS=30
# ADMIN_MCP_REQUEST_STATE_SECRET=replace-with-distinct-32-byte-request-state-secret
