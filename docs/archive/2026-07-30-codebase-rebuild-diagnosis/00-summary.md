# 00 · 요약

기준 commit `6adc2069bafd3b9c61a5a0589c705fbf85b0e9d6` (main, 2026-07-29, working tree clean) · 조사일 2026-07-30

코드 817파일 / 86,337 SLOC (프로덕션 51,659 · 테스트 34,678) · 문서 419파일 / 61,086줄 · commit 1,193

## 한 줄 결론

이 저장소의 **설계 규율과 도구 수준은 평균 이상**이고 (`any` 0건, `as unknown as` 0건, TODO 0건, depcruise 위반 0, 브랜드 타입 + 컴파일 시 단정, 원클릭 온보딩), 문제는 **그 규율을 지켜주는 자동화가 4일간 전부 죽어 있었다**는 것이다. 필요한 것은 재작성이 아니라 게이트 복구와 경계 계약의 정밀화다. 총 공수 **29.5 MD**, 그중 처음 **2.4 MD**가 위험의 절반 이상을 제거한다.

## 축별 점수

| 축                      | 점수    | 결정적 근거                                                                          |
| ----------------------- | ------- | ------------------------------------------------------------------------------------ |
| 1 가독성·의도 표현      | 3       | TODO 0건 (+) / DTO를 도메인 이름으로 별칭 11곳, 용어집 13줄 (−)                      |
| 2 인지 부하             | 2       | 1파일 1,384줄에 함수 42개, `apps/api/src` 최상위 디렉터리 19개                       |
| 3 응집도·결합도         | 2       | 시간대 7곳 5표현, purge 29파일 / 패키지 그래프 비순환 (+)                            |
| 4 관심사 분리·모듈 경계 | 3       | depcruise 7규칙 자동 강제 (+) / 모듈 `./schema` 전면 공개 (−)                        |
| 5 추상화 품질           | 3       | 포트·어댑터 일관 (+) / 에러 5중 변환, 1줄 재수출, 미사용 파라미터 매퍼 (−)           |
| 6 과잉·중복             | 2       | 무동작 플래그 9곳, 사장 브랜드 2종, 2.38MB 바이너리, 무효 lint 15개                  |
| 7 삭제 용이성           | 2       | "학습자 삭제" 정책 변경에 29파일 관여                                                |
| 8 예측 가능성·에러 처리 | 3       | `catch → Result` 규율 31곳 (+) / 원인 폐기 25/31 (−)                                 |
| 9 타입 안정성·계약      | **4**   | `any` 0, `as unknown as` 0, 브랜드 + `.typecheck.ts` 단정, 커스텀 lint 룰            |
| 10 신뢰성·운영          | 3       | graceful shutdown·백업·감사·엔트로피 검증 (+) / 초안 유실 경로, 감사 실패 무흔적 (−) |
| 11 테스트               | 2       | 912 통과 (+) / 케이스당 46.7줄, 병렬 조립 복제, 무의미 테스트 (−)                    |
| 12 피드백 루프·자동화   | **1**   | **CI 14개 job 전부 첫 스텝 실패**, 로컬 build 실패, knip 실패                        |
| **평균**                | **2.5** |                                                                                      |

12축이 1점인 이유는 도구 부재가 아니다. **도구는 잘 갖춰져 있고 실행되지 않는다.**

## 상위 5개 위험

| #   | 위험                                               | 근거 (실측)                                                                                                                                                                                                    | 방치 6개월 후                                                                                                                                  |
| --- | -------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | **CI가 실행되지 않는다**                           | `bun run check:toolchain` → `EXIT=1 / Script not found`. workflow 참조 14곳. 삭제 commit `c8e22f26`(2026-07-26)                                                                                                | 정적 검사 8종 + E2E 3프로젝트 + Lighthouse + k6가 전액 손실. 게이트 복구 시점에 누적 위반이 수백 건이 되어 팀이 게이트를 다시 끄는 선택을 한다 |
| 2   | **감사·개인정보 삭제 실패가 흔적을 남기지 않는다** | `catch → err` 31곳 중 25곳이 `cause` 폐기. 감사 저장 실패 5곳(`audit-event-drizzle-repository.ts:25,46,54,69,89`) 전부 로그 없음                                                                               | 규제 대응 국면에서 "기록이 없다"를 증명해야 한다. 장애 원인 규명 시간이 계속 증가                                                              |
| 3   | **학습자 초안이 탭 종료 시 유실될 수 있다**        | `use-lesson-draft-sync.ts:459-468`이 언로드 flush를 일반 fetch로 수행. 리포 전체 `keepalive`·`sendBeacon` **0건**. `docs/engineering/lesson-runtime.md:41`은 이를 계약으로 서술. E2E는 탭 종료를 검증하지 않음 | 자동 저장이라는 제품 약속이 조용히 깨진 상태로 유지. 사용자 신뢰 손실은 계측되지 않는다                                                        |
| 4   | **개인정보 미삭제가 컴파일 에러 없이 발생한다**    | `apps/api/src/adapters/identity/learner-data-purge.ts:5,14`가 타 모듈 schema를 직접 import해 DELETE. purge 관여 프로덕션 파일 29개. depcruise 위반 0으로 통과                                                  | 새 모듈이 학습자 데이터를 저장할 때마다 누락 위험 선형 증가. 규칙이 아니라 기억에 의존                                                         |
| 5   | **도메인 개념의 정본이 없다**                      | 학습 날짜 경계가 7곳 5표현(그중 4곳은 타입 검증 밖의 SQL 문자열). `docs/glossary.md` 13줄. 식별자 검증 강도 2등급                                                                                              | 개념당 표현 수가 계속 증가해 정책 변경이 사실상 불가능해진다. 신규 개발자가 "이 두 타입이 같은 것인가"를 매번 코드로 추적                      |

근본 원인 5개(R-1~R-5)와 각 원인이 만든 증상 목록은 [02-findings.md](./02-findings.md) Phase 3 참조.

## 3단계 로드맵

| 단계                              | Step  | 목표                                                                                           | 공수        | 종료 시점의 상태                                                                 |
| --------------------------------- | ----- | ---------------------------------------------------------------------------------------------- | ----------- | -------------------------------------------------------------------------------- |
| **1단계** 게이트 복구와 위험 제거 | 0~4   | CI·build·knip 복구 · 무비용 삭제 · 초안 유실 차단 · 관측 복구 · 죽은 기능 제거                 | **7.2 MD**  | 변경이 자동 검증되는 상태. 사용자 데이터 손실 경로 0. 모든 실패가 `cause` 보유   |
| **2단계** 경계 정본화             | 5~9   | 도메인 언어 정본 · 조립 정리 · 모듈 표면 4개로 축약 · 데이터 경계 컴파일 강제 · 리포팅 읽기 뷰 | **14.8 MD** | 타 모듈 테이블 쓰기가 컴파일 에러. 시간대·식별자 정본 1곳. 공개 subpath 170 → 95 |
| **3단계** 표현 계층과 테스트      | 10~12 | 오류 변환 축약 · 프론트엔드 모델 경계 · 테스트 셋업 공유                                       | **7.5 MD**  | 에러 변환 5회 → 2회. UI가 전송 DTO와 분리. 테스트 SLOC −25%                      |
|                                   |       |                                                                                                | **29.5 MD** |                                                                                  |

각 Step은 단독 배포·롤백 가능. wire 계약을 바꾸는 Step은 없다(Step 5의 ID 검증 강화만 행위 변경이며 스테이징 검증 필요). Step별 검증 명령·롤백·선행조건은 [04-roadmap.md](./04-roadmap.md).

맨먼스 주의: 29.5 MD는 1인 순차 작업량이다. `Step 0 → 1 → 5 → 7 → 8 → 9` 의존 사슬은 인원으로 압축되지 않는다. 2인 투입 시 실질 단축은 약 30%.

## 기대 효과

| 지표                                      | 현재 (실측)                       | 목표              | 측정 방법                             |
| ----------------------------------------- | --------------------------------- | ----------------- | ------------------------------------- |
| 온보딩: 문서만 보고 빌드 성공             | **불가** (`bun run build` EXIT 1) | 가능              | `bun run setup && bun run build` 성공 |
| 온보딩: 명령 수                           | 2 (`setup`, `dev`)                | 유지              | 이미 목표 달성 — 문제는 build 게이트  |
| 변경 리드타임: 시간대 정책 수정 지점      | 7곳 / 5패키지 (4곳은 SQL 문자열)  | 1곳               | `git grep` 카운트                     |
| 변경 리드타임: 학습자 삭제 정책 수정 지점 | 14곳 (`apps/api`)                 | 6곳               | `git grep -l` 카운트                  |
| 변경 리드타임: AI 피드백 실패 1건 추가    | 3~5곳                             | 2곳               | 코드 리뷰                             |
| 결함 검출 시점: 타 모듈 스키마 드리프트   | 런타임                            | migration 시점    | Step 9 view 생성 실패                 |
| 결함 검출 시점: purge 누락                | 미검출                            | 조립 시 타입 에러 | Step 8 포트 레지스트리                |
| 관측: 실패 원인 보존율                    | 6 / 31 (19%)                      | 31 / 31 (100%)    | `git grep -c 'err({ cause'`           |
| 테스트 밀도                               | 46.7줄 / 케이스                   | 37줄 이하         | SLOC ÷ 케이스 수                      |
| 문서 컨텍스트                             | 61,086줄                          | 20,300줄 (−67%)   | `repomix:docs` 출력                   |
| 검증 게이트 실행률                        | **0%** (14 job 실패)              | 100%              | CI job 성공 수                        |

**결함율은 현재 측정할 수 없다.** CI가 실행되지 않으므로 회귀가 어느 게이트에서 잡히는지에 대한 데이터가 없다. 1단계 완료 후부터 측정이 시작되며, 그 자체가 1단계의 핵심 산출물이다. 결함율 목표를 지금 숫자로 제시하는 것은 근거 없는 주장이 된다.

## 즉시 착수 (2.4 MD)

| #         | 작업                                                                                                   | 우선순위 점수 |
| --------- | ------------------------------------------------------------------------------------------------------ | ------------- |
| Q-01      | CI `check:toolchain` 참조 14곳 정리 + 재발 방지 검사 신설                                              | 125.0         |
| Q-02      | `getFirstLessonStep` 미사용 export 제거                                                                | 90.0          |
| Q-03      | `public-url.ts`에 loopback 예외 추가 → 로컬 build 통과                                                 | 53.3          |
| Q-04      | `no-restricted-imports` 패턴 `@test/*` → `@/test/*` 정정                                               | 30.0          |
| Q-05~Q-10 | 바이너리 2.38MB 삭제, env shim 제거, repomix 범위 축소, 죽은 설정 4곳, 무효 lint 15개, 동어반복 테스트 | 10.0~20.0     |

Q-01·Q-02·Q-03을 먼저 하지 않으면 나머지 모든 작업의 검증이 신뢰할 수 없다. 상세는 [07-quick-wins.md](./07-quick-wins.md).

## 지켜야 할 것

재구축 과정에서 실수로 사라지기 쉬운 12개 결정을 [05-deletion-backlog.md](./05-deletion-backlog.md) E절에 명시했다. 특히 주의할 셋.

1. **`dependency-cruiser.config.mjs:135-160`** — 경계 규칙을 각 `package.json` `exports`에서 **파생**한다. Step 7에서 exports를 줄일 때 이 파생 메커니즘을 손대면 규칙이 굳어 낡기 시작한다.
2. **`packages/**/\*.typecheck.ts`** — `@ts-expect-error`로 브랜드 분리와 exhaustiveness를 컴파일 시 단정. 런타임 테스트로 대체할 수 없다. Step 10의 오류 축약이 이 안전망 위에서만 안전하다.
3. **`container-cleanup.ts:57`, `server-lifecycle.ts:274,543`, `create-container.ts:429`, `main.ts:69`, `auth/learner/server.ts:84`의 빈 `catch`** — 정리 실패 보고가 원래 오류를 가리지 않게 하는 **정당한 설계**다. Step 3(F-22, `cause` 보존)에서 이 6곳을 건드리지 말 것.

## 문서 구성

| 문서                                               | 내용                                                                                                |
| -------------------------------------------------- | --------------------------------------------------------------------------------------------------- |
| [01-inventory.md](./01-inventory.md)               | 지형도, 의존성 그래프(Mermaid), 핫스팟, 도메인 용어 대조, 실행 결과 실측                            |
| [02-findings.md](./02-findings.md)                 | 발견 33건 (ID·위치·증상·원인·원칙 태그·심각도·영향·공수·권고), 축별 채점과 전후 예시, 근본 원인 5개 |
| [03-target-design.md](./03-target-design.md)       | To-Be 경계·계약·폴더 구조, 대안 2건 비교와 권고, 도입하지 않을 것                                   |
| [04-roadmap.md](./04-roadmap.md)                   | 우선순위 계산표, Step 0~12 (목표·작업·영향 파일·검증·롤백·공수·선행조건), 배포 가능성 점검          |
| [05-deletion-backlog.md](./05-deletion-backlog.md) | 삭제 26건 (A 즉시 / B 죽은 기능 / C 통합 / D 소유자 확인) + 보존 12건, 최종 규모 목표               |
| [06-conventions.md](./06-conventions.md)           | 네이밍·오류·경계·테스트·게이트 규칙과 강제 수단, 리뷰 체크리스트, 정정할 기존 문서                  |
| [07-quick-wins.md](./07-quick-wins.md)             | 1일 이내 저위험 개선 10건, 실행 순서                                                                |
| [progress.md](./progress.md)                       | 실행 추적 체크리스트 — 마일스톤 5개 · 티켓 15개 · 233항목                                           |
