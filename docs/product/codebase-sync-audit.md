# 코드베이스 동기화 점검

## 목적

이 문서는 유저 스토리와 요구사항 문서를 현재 코드베이스와 맞추기 위해 역추적한 결과를 기록한다. 제품 문서의 세부 규칙이 코드와 어긋나거나 누락될 때, 어떤 요구사항에 반영해야 하는지 찾는 기준으로 사용한다.

## 점검 기준

- 기준일: 2026-06-20
- 제외 범위: `/Kwep`
- 포함 범위: `apps`, `packages`, `docs`의 정식 문서
- 점검 단위: API 라우트, core 서비스, 도메인 정책, 저장소 구현, web/admin 기능 화면
- 문서 반영 방식: 새 기능을 중복 문서로 만들지 않고 기존 유저 스토리와 요구사항에 하위 규칙을 보강한다.

## 학습자 비즈니스 로직

| 코드 영역        | 확인한 제품 규칙                                                                                                                                                     | 반영 문서                                           |
| ---------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------- |
| 학습자 인증      | Google 로그인만 제공하고, 세션 생성 후 프로필이 없으면 active 프로필을 1회 생성한다. 안전하지 않은 `next` 경로는 기본 경로로 대체한다.                               | `US-LRN-1`, `REQ-LRN-1`                             |
| 보호 라우트      | active 학습자 세션이 있어야 학습자 API를 사용할 수 있다. suspended/deleted 상태는 보호 API에서 차단된다.                                                             | `REQ-LRN-1`, `docs/engineering/auth-permissions.md` |
| 코스 조회        | 학습자 코스 목록과 상세는 active 코스, 유닛, 레슨, 스텝만 노출한다.                                                                                                  | `US-LRN-3`, `US-LRN-4`, `REQ-LRN-3`                 |
| 진행 계산        | 코스의 첫 미완료 레슨만 `available`이고 이후 레슨은 `locked`다. 완료 수와 전체 레슨 수로 진행률을 계산한다.                                                          | `US-LRN-2`, `REQ-LRN-2`, `REQ-LRN-4`                |
| 레슨 진행 저장   | 레슨 시작, 스텝 답변, 레슨 완료는 활동일을 기록한다. 완료 재호출은 완료 수를 중복 증가시키지 않는다.                                                                 | `US-LRN-5`, `REQ-LRN-4`                             |
| 쓰기 답변        | 쓰기 답변은 공백만 있는 값을 유효한 답변으로 저장하지 않는다.                                                                                                        | `US-LRN-6`, `REQ-LRN-5`                             |
| 정답 확인형 활동 | 객관식, 빈칸, 구간 선택, 순서, 매칭, 분류는 현재 스텝에 존재하는 값만 저장한다. 매칭은 오른쪽 선택지를 결정적으로 섞고 한 오른쪽 선택지는 한 왼쪽 항목에만 연결한다. | `US-LRN-11`, `REQ-LRN-10`                           |
| AI 코칭          | AI 피드백은 `AI_FEEDBACK` 스텝에서만 생성한다. 완료된 피드백은 사용자, 레슨, 스텝 기준 최대 3회이며 제공자 실패는 시도 완료로 저장하지 않는다.                       | `US-LRN-7`, `REQ-LRN-6`                             |
| 프로필 통계      | 완료 레슨, 전체 active 레슨, 최근 활동일, 연속 학습일, 진행률을 학습 진행과 활동일로 계산한다.                                                                       | `US-LRN-8`, `REQ-LRN-7`, `REQ-LRN-2`                |

## 어드민 비즈니스 로직

| 코드 영역         | 확인한 제품 규칙                                                                                                                                | 반영 문서               |
| ----------------- | ----------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------- |
| 어드민 인증       | 관리자 인증은 email/password 기반 Better Auth를 사용하고 operator와 owner role을 구분한다.                                                      | `US-ADM-1`, `REQ-ADM-1` |
| 대시보드          | 총 사용자, 최근 7일 활성 사용자, 오늘/7일 가입, 누적 완료 레슨, active 콘텐츠 수, 최근 활동 5명을 제공한다. deleted 학습자는 집계에서 제외한다. | `US-ADM-2`, `REQ-ADM-2` |
| 분석              | 최근 30일 가입/완료 추이, 연속 학습일 구간, 완료율이 낮은 레슨 8개, 레슨별 시작/완료/완료율/이탈률을 제공한다.                                  | `US-ADM-2`, `REQ-ADM-2` |
| 코스 목록         | 제목/설명 검색, 카테고리, 상태, 페이지 크기, 페이지네이션을 지원한다. 페이지 크기 상한은 100이다.                                               | `US-ADM-3`, `REQ-ADM-3` |
| 새 코스 생성      | owner는 기본 코스, 기본 유닛, 기본 레슨, 읽기 스텝, 쓰기 스텝을 포함한 active 코스 aggregate를 생성한다.                                        | `US-ADM-3`, `REQ-ADM-3` |
| 코스 보관         | owner는 코스를 row 삭제하지 않고 `archived` 상태로 전환한다. 이미 보관된 코스는 not found처럼 처리한다.                                         | `US-ADM-3`, `REQ-ADM-3` |
| 코스 미리보기     | 관리자 세션이 있으면 active 코스의 active 유닛, 레슨, 스텝만 읽기 전용으로 볼 수 있다. 저장형 편집은 제공하지 않는다.                           | `US-ADM-3`, `REQ-ADM-3` |
| 사용자 목록       | 이름/이메일 검색, 상태 필터, 정렬, 페이지네이션을 지원한다. 기본 정렬은 최근 접속이고 페이지 크기 상한은 100이다.                               | `US-ADM-4`, `REQ-ADM-4` |
| 사용자 상태 운영  | owner만 active/suspended 상태 변경과 deleted 상태 전환을 수행한다. 상태 변경은 `deletedAt`을 비우고, 삭제 요청 처리는 `deletedAt`을 기록한다.   | `US-ADM-4`, `REQ-ADM-4` |
| 운영 설정         | 공지, 배너, 약관, 개인정보처리방침을 key-value로 저장한다. 저장과 초기화는 owner만 수행한다.                                                    | `US-ADM-5`, `REQ-ADM-5` |
| 콘텐츠 초기화     | seed에 있는 콘텐츠는 active로 재정렬하고 seed에 없는 코스, 유닛, 레슨, 스텝은 archived로 전환한다. 새 revision과 변경 수를 반환한다.            | `US-ADM-5`, `REQ-ADM-5` |
| 어드민 내비게이션 | 대시보드, 코스, 사용자, 분석, 설정 화면을 보호된 어드민 shell 안에서 제공한다.                                                                  | `US-ADM-6`, `REQ-ADM-6` |

## 보강한 누락 항목

- AI 코칭의 최대 완료 시도 수 3회와 제공자 실패 시도 미저장 규칙.
- 매칭 활동의 결정적 오른쪽 선택지 섞기와 1:1 선택 전환 규칙.
- 레슨 완료 재호출 시 완료 수를 중복 증가시키지 않는 활동일 집계 규칙.
- 어드민 대시보드와 분석에서 deleted 학습자를 제외하는 집계 규칙.
- 어드민 사용자 목록의 기본 정렬, 상태 필터 해석, 페이지 크기 상한.
- 새 코스 생성 시 기본 aggregate와 콘텐츠 초기화 시 seed 외 콘텐츠 보관 규칙.

## 남은 비문서화 대상

현재 점검 기준에서 제품 요구사항으로 승격해야 할 미문서화 비즈니스 로직은 없다. `packages/env`, `packages/hono`, `packages/http-client`, `packages/logger`, `packages/ui`, `packages/config`, Storybook은 제품 요구사항이 아니라 런타임, 인프라, UI 기반 패키지로 분류한다.
