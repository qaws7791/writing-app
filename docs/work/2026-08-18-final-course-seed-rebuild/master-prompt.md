# 최종 코스 시드 재구축 마스터 프롬프트

이 문서는 저장소를 모르는 외부 AI에게 한 턴으로 `content-seed-data.json`을 만들게 하는 요청문이다. 현재 제품 사실의 권위 소스가 아니다. 시드 값을 저장소에 넣기 전에 5개 추가 스텝 타입의 제품 계약을 맞추는 작업은 이 파일 범위 밖이다.

한 턴에 스텝까지 쓰게 한 이 문서는 규모 때문에 실패했다. 현재 실행문은 [`master-prompt-v2.md`](./master-prompt-v2.md)다. 이 파일은 이전 요청문을 보존한다.

## 사람용 실행 안내

1. 웹 검색과 파일 생성 도구를 쓸 수 있는 새 대화에서 실행한다. Claude, ChatGPT 등 모델은 상관없다.
2. 아래 「마스터 프롬프트」울타리 안 전체를 첫 메시지로 한 번만 보낸다. 울타리 밖의 이 안내 문단은 보내지 않는다.
3. 실행 중에는 코스 수, 목차, 중간 초안을 승인하지 않는다.
4. 성공은 다운로드 가능한 `content-seed-data.json`과 짧은 검증 보고뿐이다.
5. 채팅 본문에 대량 JSON, 일부 코스, placeholder, “계속할까요?”가 나오면 실패다. 같은 대화에서 이어 쓰지 말고 프롬프트를 보완한 뒤 새 실행을 한다.
6. 받은 파일을 바로 권위 시드에 덮어쓰지 않는다. 구조·국어·저작권 검수를 별도 위치에서 마친 뒤에만 반영한다.
7. 지금 저장소 시드 매퍼는 스텝 타입 8개만 로드한다. 이 프롬프트가 가정하는 13개 가운데 `true_false`, `sentence_build`, `transcribe`, `error_correct`, `paragraph_organize`는 시드 적용 전에 DTO·답안·매퍼·학습자 화면·관리자 폼을 맞춰야 한다.

## 마스터 프롬프트

아래 울타리 안 전체를 새 대화의 첫 메시지로 보낸다.

````text
<role>
당신은 한국어가 모어인 일반 성인을 위한 글쓰기·읽기·사고·정보 문해 교육과정을 설계하고 집필하는 편집위원회다. 위원회에는 국어학자, 한국어교육 연구자, 작문·수사학을 가르치는 교수, 실무 글을 오래 고쳐 온 편집자가 있다.

목표는 학습자가 좋은 작문 강의나 단행본을 한 절씩 풀어 활동으로 만든 듯한, 문장 단위로 공이 들어간 시드 콘텐츠를 만드는 것이다. 실제 전문가 검수를 받았다고 주장하지 마라. 확인한 근거와 이번에 새로 쓴 예문·문항을 구분하라.

한 코스, 한 유닛, 한 레슨, 한 스텝마다 삭제하면 빈 자리가 보여야 한다. 제목만 바꿔 같은 껍데기를 돌리는 프로토타입은 제출 불가이다.
</role>

<mission>
웹에서 신뢰할 수 있는 자료를 조사하고 분석한 뒤, 한국어 모어 성인이 독립적으로 수강할 수 있는 교육 코스 10개를 탑다운으로 설계하라. 각 코스는 4주(1개월) 학습 경로다. 코스마다 유닛, 레슨, 모든 학습 스텝을 완성하고, 아래 저장소 계약에 맞는 UTF-8 JSON 파일을 생성하라.

최종 필수 산출물은 다운로드 가능한 파일 하나다.

- 파일명: `content-seed-data.json`
- 루트 값: 코스 객체의 JSON 배열. 길이는 정확히 10
- 형식: 유효한 JSON, 들여쓰기 2칸, 마지막 개행 포함
- 금지: JSON 주석, trailing comma, 생략 기호, placeholder, TODO, “예시는 동일”, 중간 설계안, 채팅 본문에 대량 JSON 붙여 넣기

대화 본문에 대용량 JSON을 붙이지 말고 파일로 첨부하라. 최종 본문에는 파일 링크와 코스·유닛·레슨·스텝 수, 파일 크기, SHA-256, 검증 통과 여부만 간단히 보고하라. 파일 생성 도구를 사용할 수 없다면 작업을 일부 출력하지 말고, 완전한 파일을 만들 수 없어 실패했다는 사실만 보고하라.
</mission>

<autonomy>
이 요청은 한 턴 안에 완결해야 한다. 필요한 웹 검색, 원문 열람, 내부 메모, 계산, 파일 작성과 비파괴 검증을 스스로 수행하라. 중간 승인을 요청하거나 일부 코스만 제출하거나 다음 메시지를 요구하지 마라.

최종 파일을 만들기 전에는 조사·분석·설계·집필·품질 감사·스키마 검증을 계속하라. 내부 계획과 상세 추론은 최종 응답에 노출하지 않는다.

규모가 크다. 채팅에 JSON을 쌓지 말고, 작업 공간에서 코스 단위로 파일에 추가하라. 한 코스를 쓴 뒤 그 코스의 스키마와 품질을 검사하고, 통과한 뒤에만 다음 코스로 가라. 미완성 코스를 병렬로 늘어놓지 마라.
</autonomy>

<product>
글결은 한국어 글쓰기를 작은 레슨과, 레슨과 분리된 쓰기 과제로 익히는 학습 앱이다.

- 핵심 학습자: 한국어가 모어인 일반 성인. 어린이·수험생·특정 직업 연수생을 기본 독자로 두지 않는다.
- 코스: 카탈로그에서 고르는 한 학습 주제. 학습자는 코스를 시작하고 레슨을 진행한다.
- 레슨: 몰입형 전체 화면에서 스텝을 한 장씩, 배열 순서대로만 전진한다. 같은 코스 안 다른 레슨은 선행 완료 없이 열 수 있다.
- 쓰기 과제: 코스·유닛·레슨·스텝 밖의 별도 영역이다. 이 JSON에 넣지 않는다. 자유 작문 스텝 `write`와 AI 코칭 스텝 `ai_feedback`는 금지다.
- `transcribe`는 제시된 원문을 그대로 재현하는 받아쓰기이다. 자유 작문이 아니다.
- `READING`과 `COMPARE`는 확인만 하고 채점하지 않는다. 나머지 11개 타입은 서버가 채점한다.
- 화면 문구와 학습 자료는 한국어다.
</product>

<learner_and_scope>
- 코스 독립성: 다른 코스의 선이수를 전제하지 않는다. 필요한 개념은 그 코스 안에서 가르친다.
- 코스 수: 정확히 10개.
- 각 코스: 4주 독립 경로. 유닛 4개(1유닛 = 1주). 주당 레슨 4~5개(평일 한 회). 코스당 레슨은 대략 16~20개.
- 레슨당 스텝: 15~20개. 수를 채우려고 같은 활동을 반복하지 않는다.
- 포함 범위: 어휘, 문장 이해, 문법·표기, 독해, 핵심 내용, 추론, 비판·분석·통합, 창의적 발상, 주제 설정, 내용 생성, 조직, 일관성·응집성, 명료한 표현, 설명, 논증, 독자, 목적과 장르, 자기 점검, 수정, 피드백 판단, 정보 탐색, 출처 신뢰성, 출처 통합·귀속, 디지털·AI 매체 문해.
- 제외 범위: 특정 직업 전용 코스, 입시 대비, 문학 창작 전용, 학술 논문 작성 전용, 어린이 받아쓰기 교재 문체.
- 배경지식은 독립 코스로 만들지 않는다. 과제에 필요한 내용 자원으로만 해당 레슨 안에 둔다.
- 코스는 서로 다른 핵심 학습 성과를 가진다. 이름만 다른 중복 코스를 만들지 않는다.
- 읽기·이해·사고·정보 판단은 직접 쓰기 결과물 없이도 독립된 목표가 될 수 있다.
</learner_and_scope>

<coverage_competencies>
10개 코스가 아래 24개 수행 공간을 빠짐없이 나누게 하라. 코스 JSON에 역량 ID 필드를 넣지 마라. 내부 설계에서 코스마다 주역량 1~3개를 배정하고, 한 주역량은 한 코스에만 둔다. 보조 역량은 여러 코스가 필요한 범위만 자체 연습한다.

1. 어휘 의미·사용: 문맥에서 뜻·용법·관계를 구별하고 의미를 보존한 단어를 고른다. 어휘량 암기만은 목표가 아니다.
2. 문장 의미 구성: 낱말·통사·지시·논리 관계를 통합해 문장 의미와 모호성을 판단한다.
3. 문법·표기 적용: 현행 규범의 원칙·허용·예외를 문맥에 적용한다. 문체 취향과 섞지 않는다.
4. 읽기 의미 구성: 목적에 따라 낱말·문장·구조·지식을 조정해 글의 표상과 이해를 만들고 점검한다.
5. 근거 기반 추론: 명시되지 않은 관계를 텍스트와 자료로 정당화한다. 자유연상은 제외한다.
6. 비판적 판단: 쟁점·가정·근거·대안을 비교하고 결론을 보류하거나 수정한다. 무조건 의심은 제외한다.
7. 구조적 분석과 통합: 자료를 목적에 맞는 요소와 관계로 분해하고 공통 구조로 통합한다.
8. 창의적 사고: 새롭고 적절한 아이디어를 다양하게 생성·평가·개선한다.
9. 수사적 주제 설정: 목적·독자·제약으로 쓸 문제의 범위를 정한다. 제목 고르기만은 제외한다.
10. 내용 생성: 목적에 관련된 아이디어와 근거 후보를 만들고 고른다. 검증 없는 복제는 제외한다.
11. 정보 조직: 정보를 위계·순서·인과·비교로 배열한다. 고정 서식 복제는 제외한다.
12. 일관성·응집성: 의미 관계를 만들고 지시·반복·접속으로 연결을 드러낸다. 접속어 수 늘리기는 제외한다.
13. 명료하고 정확한 표현: 어휘·호응·지시로 필수 의미를 보존한다. 규범 정오 전체와 거시 조직 전체를 대신하지 않는다.
14. 독자 중심 설명: 독자가 개념·과정·관계를 재구성하고 검증하도록 쓴다. 정보 나열은 제외한다.
15. 근거 기반 논증: 주장–근거–보증–반론의 관계와 결론의 범위를 세운다. 단순 순응 유도는 제외한다.
16. 독자 인식: 실제·예상 독자의 지식·입장·요구와 글이 부르는 역할을 고려한다.
17. 목적·장르 판단: 사회적 목적에 맞는 장르 관습을 고르거나 변형한다. 형식 암기는 제외한다.
18. 자기점검·조절: 목표·증거·기준으로 다음 행동을 조정한다. 막연한 재독은 제외한다.
19. 의미 중심 수정: 목표와 글의 차이를 의미·조직·표현의 실제 변화로 해소한다. 표면 교정만은 제외한다.
20. 피드백 활용: 자기·동료·도구의 조언을 해석·평가하고 채택·변형·거부한다.
21. 반복적 정보 탐색: 정보 필요에 맞춰 질의와 도구를 바꾸고 중단 기준을 둔다.
22. 정보 신뢰성 판단: 출처·주장·근거를 독립 자료와 비교해 신뢰 범위를 정한다. 외양 체크리스트 합산은 제외한다.
23. 출처 통합·귀속: 외부·AI 자료의 의미를 보존해 넣고 출처·변형·사용 범위를 표시한다.
24. 디지털·AI 매체 문해: 도구의 기능·한계·위험을 이해하고 결과를 검증하며 책임 있게 사용한다. 기기 조작만은 제외한다.
</coverage_competencies>

<research_protocol>
먼저 자료를 수집하고 분석하되, 조사 메모 자체는 최종 파일에 넣지 않는다.

자료 우선순위:
1. 최신 국립국어원 어문 규범·해설·실태 조사·공공언어 자료, 교육부·국가교육과정정보센터의 국어과 교육과정, 한국교육과정평가원 등 공공 연구
2. 동료 심사 학술 논문, 체계적 문헌고찰·메타분석, 대학·연구기관 보고서
3. OECD PIAAC·PISA, UNESCO 매체·정보 문해, 미국 IES/What Works Clearinghouse 작문 지도, ACRL, Civic Online Reasoning 등 공신력 있는 국제 자료
4. 저자와 출판 주체를 확인할 수 있는 국어학·작문교육·수사학 단행본과 공식 강의

반드시 실행일 기준으로 다시 확인할 자료:
- 국립국어원 현행 한글 맞춤법·표준어 규정 및 해설
- 국립국어원 최신 국민 국어능력·글쓰기 능력 조사와 쉬운 공공언어 자료
- 2022 개정 국어과 교육과정 고시본과 성취수준 자료
- OECD PIAAC Cycle 2 literacy framework
- IES/WWC 작문 지도 공식 자료와 후속 문서
- 작문 인지 과정, 자기조절, 수정, 응집성, 독자·수사적 상황, 장르 교육의 핵심 연구
- UNESCO MIL, ACRL, Civic Online Reasoning의 최신 공식 자료

채택 규칙:
- 공식 원문, DOI 논문, 저자·기관 저장본을 우선한다.
- 검색 결과 요약만 읽고 원문 전체를 확인한 것처럼 쓰지 않는다.
- 책·유료 논문·강의는 확인 가능한 목차·미리보기·초록·공식 소개의 범위만 사용한다.
- 일반 블로그, 커뮤니티, 익명 자료, SEO 요약문은 근거로 쓰지 않는다.
- 핵심 원리는 가능한 경우 독립된 두 자료 이상으로 교차 확인한다.
- 한국어 고유 규범과 실제 사용 판단에는 국내 권위 자료를 반드시 포함한다.
- 어린이·청소년·영어권 연구를 일반 성인 한국어 학습자에게 옮길 때는 전이 한계를 내부적으로 표시하고 효과를 단정하지 않는다.
- 규범, 경험 연구의 경향, 전문가 권고, 이번 교육과정의 설계 판단을 같은 확실성으로 표현하지 않는다.
- 상충하는 자료가 있으면 적용 조건을 구분하고 하나를 임의로 사실처럼 고르지 않는다.
- 존재하지 않는 문헌, DOI, 통계, 페이지, URL을 만들지 않는다. 실제로 연 자료만 인용한다.
- 원문을 길게 전재하지 않는다. 예문·지문·선택지·참조 답안은 원리를 반영해 새로 집필한다.

수집 종료 조건:
- 위 24개 수행 공간이 근거 지형에 포함된다.
- 핵심 원리의 교차 확인과 한국어 고유 근거가 확보된다.
- 상충점, 적용 대상, 전이 한계를 구분할 수 있다.
- 반복 검색에서 코스 구조를 바꿀 새로운 핵심 원리가 거의 나오지 않는다.
- 근거가 부족한 주제는 억지로 코스에 넣거나 강한 표현으로 쓰지 않는다.
</research_protocol>

<curriculum_design>
조사 후 다음 순서로 내부 설계를 완료하라.

1. 출처에서 가르칠 개념, 원리, 수행 절차, 흔한 오류, 평가 기준, 적용 한계를 추출한다.
2. 24개 수행의 중복·상하·인접 관계를 분석한다.
3. 코스마다 주역량 1~3개를 배정하는 행렬을 만든다.
4. 독립성, 근거 충분성, 고유 성과, 중복 정도로 10개를 확정한다.
5. 전체 카탈로그의 제목, 설명, 카테고리, 배열 순서, visualKey를 먼저 확정한다. 배열 순서는 선수 관계가 아니다.
6. 각 코스의 4주 목차(유닛·레슨 제목과 관찰 가능한 목표)를 확정한다.
7. 반드시 첫 코스의 모든 유닛·레슨·스텝을 완성하고 자체 검수한 뒤 두 번째 코스로 넘어간다.
8. 모든 코스가 끝나면 전체 카탈로그의 중복, 누락, 용어, 난이도, 예문 재사용을 다시 감사한다.

유닛 = 1주:
- 1주: 기초 개념과 그 영역에서 흔한 오류
- 2주: 핵심 절차와 안내된 예시
- 3주: 새 장르·맥락으로의 전이
- 4주: 통합, 자기점검 기준, 앞선 주의 지연 연습

레슨:
- 한 회에 관찰 가능한 수행 하나
- `title`: 그 수행이 드러나는 한국어 제목
- `desc`: 학습자가 무엇을 하게 되는지. 마케팅 문장 금지
- `time`: `20분`부터 `35분` 사이. 실제 15~20스텝 분량과 맞춘다. 모든 레슨을 같은 분으로 통일하지 않는다
- `summary`: 2~5개의 재사용 판단 기준. 제목 반복 금지. 주제와 무관한 메타 문장 금지
- `cat`: 넣으면 해당 유닛 제목을 재사용할 수 있다

카테고리는 다음 다섯 값 중 하나만 쓴다. `미분류`는 쓰지 않는다.

- `언어와 읽기`
- `구성과 표현`
- `사고와 발상`
- `독자와 쓰기 과정`
- `정보와 AI 문해`

visualKey는 다음 다섯 값만 쓴다. 여러 코스가 같은 값을 공유할 수 있다.

- `basic-sentence-writing`
- `grammar-complete`
- `essay-writing`
- `creative-writing`
- `expression`
</curriculum_design>

<lesson_rhythm>
레슨당 스텝은 15~20개다. 한 목표를 여러 예시로 깊게 연습한다. 유닛 전체를 한 레슨에 넣지 않는다.

설명과 확인이 번갈아 나오게 하라. 주제(맞춤법, 장르, 실무 등)를 특정 스텝 타입에 묶지 마라. 같은 원리를 `reading`으로 펼치고 `compare`로 전후를 보여 주고 `true_false`로 판별하고 `error_correct`로 고치고 `fill_blank`로 한 요소만 채우는 식의 재사용이 바람직하다.

권장 리듬(타입 고정이 아니다):
1. 개념을 펼치는 `reading`: 정의, 경계(무엇이 아닌가), 성인 글에서 왜 실패하는지, 문장 단위로 푼 예문
2. 같은 재료의 대조 또는 첫 확인
3. 예시 A에서 서로 다른 판단 지점을 묻는 확인 여러 개
4. 예외·허용형·전이 조건을 여는 다음 설명
5. 장르·맥락이 다른 예시 B, C로 전이 확인
6. 레슨 `summary`와 같은 말로 닫는 정리

제약:
- 확인 없이 `reading`만 3개 연속하지 않는다.
- 같은 타입을 4개 이상 연속하지 않는다.
- 스텝마다 새 정보, 새 예시, 새 판단 중 하나가 있어야 한다.
- 정답은 스키마가 허용하는 결정적 값이어야 한다.
- 열린 작문처럼 여러 답이 되는 문항을 단일 정답으로 만들지 않는다.
- 15~20개를 채우려고 같은 예문과 같은 오답을 돌리지 않는다.
</lesson_rhythm>

<step_as_mechanism>
13개 타입은 학습 영역이 아니라 조작 메커니즘이다. 수백 개 코스로 확장해도 재사용할 형식이다.

금지:
- `reading` = 개념 전용, `error_correct` = 맞춤법 전용, `order` = 개요 전용처럼 타입을 주제에 고정하는 일
- 아래 7칸 예시를 체크리스트로 돌려 레슨마다 칸을 채우는 일
- 목록에 없다고 목표에 맞는 활용을 버리는 일

스키마가 고정하는 것은 화면 조작뿐이다. 그 위에 올릴 국어·작문 내용은 열려 있다.

아래 활용은 예시의 일부다. 전부도 아니고 의무 목록도 아니다. JSON의 CHOICE는 `multiple_choice`로 적는다.

READING 예시: 어휘·표현·유형학 / 맞춤법·띄어쓰기·문장부호 / 문법·문형·문장 성분 호응 / 문체·수사법·문장 교정 / 문단 구성·논리 구조 / 갈래별 글쓰기 가이드 / 실무·비즈니스·실용 글쓰기

COMPARE 예시: 초고 vs 퇴고 / 아쉬운 예 vs 좋은 예 / 어휘 뉘앙스 / 상황·대상별 어조 / 문단 구조·논리 전개 / 맞춤법·띄어쓰기·문장부호 전후 / 실무 문서·장르 실전 사례

MULTIPLE_CHOICE 예시: 어휘·표현·맞춤법 재인 / 띄어쓰기·문장부호 / 문법·문형·호응 / 문체·수사·교정 최선안 / 문단 흐름·논리·통일성 / 갈래별 선택 / 실무·퇴고·편집 실전

TRUE_FALSE 예시: 어휘·표현 유효성 / 맞춤법·띄어쓰기 판별 / 문법·호응 / 문체·수사·교정 / 문단·논리 / 갈래 규칙 / 실무·퇴고 수칙

SELECT 예시: 표기 오류 찾기 / 어휘·문맥 오용 / 문법·호응 오류 / 문장 성분·품사 식별 / 문단 구조·주제 탐지 / 문체·수사·어조 분석 / 실무·장르 핵심 정보와 수정

CATEGORIZE 예시: 어휘·표현 속성 / 표기 올바름 / 문법·문장 구조 속성 / 문체·수사·어조 / 문단·논리 속성 / 장르·상황별 글쓰기 / 실무·편집·퇴고

MATCH 예시: 어휘·의미 연결 / 표기·표현 교정 짝 / 문법·어미·조사·성분 / 문체·수사 기법 / 문단·논리·개요 / 갈래 응용 / 실전 종합

ORDER 예시: 단어·구·성분 어순 / 관용구·구절 조립 / 문장 간 접속 / 문단 내 문장 재배열 / 글 전체 문단 배열 / 갈래·장르 순서 / 프로세스·논리 체인·쓰기 절차

FILL_BLANK 예시: 문맥 어휘 / 조사·어미 / 혼동 표기 / 접속어 / 문단 맥락 요소 / 갈래 관용 틀 / 수사·어조·고급 표현

SENTENCE_BUILD 예시: 기본 어순·성분 / 조사·어미 결합 / 구문·절 조립 / 높임법·어조 변환 / 실무 문장 / 학술·논술·칼럼 문장 / 교정·수사·문장 다이어트

TRANSCRIBE 예시: 띄어쓰기 심화 / 맞춤·표기 정확성 / 문장부호·숫자·단위 / 공문서 정형 문장 체화 / 학술·법률·뉴스 용어 / 명문장·관용구 옮겨 적기 / 모범 문장 체화

ERROR_CORRECT 예시: 맞춤·표기 / 띄어쓰기 / 문법·성분 / 어휘 오남용 / 번역투·어색한 문체 / 실무 문서 / 문단 논리·맥락

PARAGRAPH_ORGANIZE 예시: 논리 전개별 문단 / 무관 문장 걸러내기 / 실무 문단 / 학술·논술·설득 / 기사·보도·설명 / 수필·에세이·자기소개 / 마케팅·SNS·콘텐츠
</step_as_mechanism>

<craft_quality>
품질 바닥선은 이 절과 아래 골드 스탠더드 JSON이다. 스키마가 맞아도 이 밀도를 못 채우면 해당 코스를 다시 쓴다.

금지하는 프로토타입 패턴. 하나라도 반복되면 그 레슨을 버리고 다시 쓴다.

- 레슨 제목만 바꿔 같은 본문·같은 예문·같은 선택지·같은 해설을 돌린다.
- `reading.body`가 “이번 레슨에서는 **[목표 문장]**”으로 시작하고, 모든 레슨에 동일한 3줄 판단 기준을 붙인다.
- `summary`가 제목을 되풀이하거나 “대상을 고정한다 / 근거와 한계를 구분한다 / 결론을 검토한다”처럼 주제와 무관한 메타 문장이다.
- 예문이 학습 목표와 무관하다. 다의어 레슨에 다의어가 없고, 강도 레슨에 정도어가 없다.
- 활동이 개념을 묻지 않고 “근거로 확인한 구간을 고르세요” 같은 절차 메타 문항만 낸다.
- 해설이 목표 문장을 다시 붙이기만 하고, 왜 이 답이 맞으며 오답이 어떤 오개념인지 말하지 않는다.
- 모든 레슨 `time`이 같고, 모든 `guide`가 “목표, 근거와 판단의 한계를 구분해 읽어 보세요”다.
- `source`에 “자체 집필”을 기계적으로 넣는다. 규범·연구를 실제로 확인한 `reading`만 `source`를 쓴다. 자체 예문에는 `source`를 생략한다.
- 아동용 문장(“철수는 학교에 갔다”), 번역투 예문, 존재하지 않는 통계·조항·URL.

계층별 공:

- 코스: 한 문장으로 끝나는 고유 테제가 있다. 목차는 주제 나열이 아니라 4주에 난이도와 장르가 쌓인다. `desc`는 수강 후 할 수 있는 판단이다.
- 유닛: 그 주의 통과 기준이 있다. 레슨 제목을 이어 붙인 이름이 아니다. 같은 예문을 주 안에서 되풀이하지 않는다. 뒤 레슨은 앞 레슨의 구분을 전제로 더 어려운 경계를 다룬다.
- 레슨: 관찰 가능한 수행 하나. 예문 세트 A→B→C는 장르나 함정이 달라야 한다.
- 스텝: 삭제하면 빈 자리가 보여야 한다. `reading`은 정의·대조·푼 예문·흔한 오류·적용 한계가 있는 한 편의 학습 문서다. 확인 스텝의 예문은 직전 설명의 원리를 새 문장에 적용한다. 오답은 교실에서 실제로 나오는 혼동이다.

국어·작문 밀도:

- 용어는 한국어학·작문교육의 뜻을 지킨다. 지시, 호응, 피수식, 주제와 화제, 응집성과 일관성, 수사적 상황, 장르 관습, 주장–근거–보증–반론을 서로 바꿔 쓰지 않는다.
- 맞춤법·표준어·띄어쓰기는 현행 국립국어원 규정과 허용형을 구분한다. 문체 취향을 규범인 것처럼 쓰지 않는다.
- 예문은 성인 생활 글이다. 직장 메일, 공지, 설명문, 후기, 건의, 짧은 칼럼, 안내 문구. 사실처럼 적은 고유명·연도·수치는 검증한다.
- `reading` 안에 나쁜 문장과 고친 문장을 직접 보여 주고, 의미·독자·문법에서 무엇이 달라지는지 문장 성분까지 짚는다.
- 해설은 정답 라벨 반복이 아니라 판단 경로다. 예: “이 구간이 조건절이라 범위가 줄어든다.” “이 유의어는 격식은 맞지만 대상이 사람이 아니라서 탈락한다.”

문체:

- 설명은 `-습니다`, 활동 지시는 `-해 보세요`를 기본으로 한다.
- 성인을 어린이처럼 대하지 않는다.
- 특정 성별·지역·세대·직업·정치 입장을 정상으로 가정하지 않는다.
- 번역투, 과장, 상투적 AI 문체, 부자연스러운 명사 나열을 피한다.
- “전문가 검수 완료”, “과학적으로 증명”, “무조건”, “항상”처럼 근거보다 강한 표현을 쓰지 않는다.

서술형 학습 자료는 `reading.body`의 CommonMark Markdown이다. 제목, 문단, 목록, 인용문을 써서 자립적인 학습 문서가 되게 한다. 원시 HTML과 깨진 Markdown을 쓰지 않는다. 다른 활동 필드에 긴 이론을 숨기지 않는다.
</craft_quality>

<gold_standard>
아래 밀도 미만은 제출 불가이다. 이 조각은 한 레슨의 앞부분이다. 전체 레슨을 이 두 스텝으로 끝내지 말고, 이 밀도로 15~20스텝을 이어가라. 이 JSON을 최종 파일에 복사해 넣지 마라. 원리를 새 예문으로 재구현하라.

```json
{
  "type": "reading",
  "title": "한 단어가 여러 뜻일 때, 이 문장이 고르는 뜻",
  "guide": "‘열다’가 물리적으로 문을 여는 동작인지, 행사를 개최하는 행위인지, 문장 안에서 무엇이 한쪽 뜻을 닫는지 표시하며 읽으세요.",
  "body": "사전에서 ‘열다’를 찾으면 문이 열리다, 모임을 시작하다, 가게를 시작하다처럼 여러 쓰임이 나란히 있습니다. 학습자가 실패하는 지점은 뜻을 외우지 못해서가 아니라, **지금 이 문장이 그 목록 가운데 어느 쓰임을 요구하는지**를 주변 성분으로 좁히지 못해서입니다.\n\n다의어는 형태는 같고 의미 사이에 관련 있는 여러 쓰임을 가집니다. 동음이의어는 형태만 같고 의미 관련은 거의 없습니다. ‘열다’의 ‘문을 열다’와 ‘설명회를 열다’는 열림·개시라는 관련을 공유하므로 다의에 가깝습니다. 이 레슨의 목표는 사전 항목을 외우는 일이 아니라, **목적어와 장소·시간 부사어가 어떤 쓰임을 남기고 어떤 쓰임을 닫는지**를 읽는 일입니다.\n\n다음 공지 한 줄을 보겠습니다.\n\n> 운영팀은 다음 주 화요일 오후 2시에 본관 3층에서 변경 안내 설명회를 열고 현장 신청을 받습니다.\n\n‘열다’의 목적어는 ‘문’이 아니라 ‘설명회’입니다. ‘설명회’는 열 수 있는 물체가 아니라 개최할 수 있는 행사입니다. ‘다음 주 화요일 오후 2시’와 ‘본관 3층’은 행사의 때와 곳이지, 여닫는 동작의 때와 곳이 아닙니다. 따라서 이 문장의 ‘열다’는 *개최하다* 쪽에 가깝고, *문을 열다* 쪽은 목적어가 이미 닫습니다.\n\n흔한 오독은 사전의 첫 항을 기본값으로 두는 일입니다. ‘열다’를 보면 손과 문을 떠올리고, 공지 전체를 물리적 동작으로 읽습니다. 그러면 ‘설명회를 연다’가 방을 개방한다는 뜻이 되어, 뒤의 ‘현장 신청을 받습니다’와 사건 구조가 어긋납니다.\n\n좋은 절차는 동사부터 뜻을 정하지 않는 일입니다.\n\n1. 목적어와 필수 부사어를 표시합니다.\n2. 각 후보 뜻이 그 결합을 허용하는지 확인합니다.\n3. 허용되지 않는 후보를 버리고, 남은 뜻으로 뒤 절이 이어지는지 확인합니다.\n\n한계도 분명히 둡니다. 문맥이 두 쓰임을 동시에 허용하는 말장난이나 제목은 이 레슨의 정답 범위가 아닙니다. 그런 문장은 후보를 닫지 못했다고 표시하고, 단정하지 않습니다."
}
```

```json
{
  "type": "select",
  "question": "다음 공지에서, ‘열다’의 뜻을 *개최하다* 쪽으로 좁히고 *문을 열다* 쪽을 닫는 근거가 되는 구간을 모두 고르세요.",
  "segments": [
    "운영팀은 ",
    "다음 주 화요일 오후 2시에 ",
    "본관 3층에서 ",
    "변경 안내 설명회를 ",
    "열고 ",
    "현장 신청을 받습니다."
  ],
  "segmentIds": [
    "agent",
    "time",
    "place",
    "object-event",
    "verb-open",
    "follow-on-action"
  ],
  "correct": [
    "time",
    "place",
    "object-event",
    "follow-on-action"
  ],
  "explanation": "뜻을 가르는 힘은 동사 ‘열고’ 자체가 아니라 결합 성분에 있습니다. 목적어 ‘변경 안내 설명회를’은 개최 대상이지 여닫을 물체가 아닙니다. 때와 곳은 행사의 일정·장소로 읽히고, 뒤 절 ‘현장 신청을 받습니다’는 개최된 행사에서 이어지는 행위입니다. 주어 ‘운영팀은’은 행위자를 알려 줄 뿐 두 쓰임을 가르지 않습니다. 동사 구간만 고르면 사전의 첫 항으로 되돌아갑니다.",
  "layout": "inline"
}
```

위 `select`가 묻는 것은 “근거 구간 고르기”라는 절차 이름이 아니라, **어느 성분이 어느 뜻을 닫는가**입니다. 모든 확인 스텝이 이렇게 그 레슨의 핵심 구분을 직접 연습해야 합니다.
</gold_standard>

<repository_contract>
이 파일은 Bun/TypeScript 학습 앱의 시드 입력이다. 당신은 저장소를 볼 수 없으므로 아래 계약만 권위로 사용하라.

```ts
type Seed = Course[] // 길이 10

type Course = {
  id: string
  title: string
  desc: string
  cat: "언어와 읽기" | "구성과 표현" | "사고와 발상" | "독자와 쓰기 과정" | "정보와 AI 문해"
  visualKey:
    | "basic-sentence-writing"
    | "grammar-complete"
    | "essay-writing"
    | "creative-writing"
    | "expression"
  units: Unit[] // 길이 4
}

type Unit = {
  id: string
  title: string
  lessons: Lesson[] // 주당 4~5
}

type Lesson = {
  id: string
  title: string
  time: `${positiveInteger}분` // 권장 20~35
  cat?: string
  desc?: string
  summary?: string[] // 권장 2~5
  steps: Step[] // 15~20
}

type Step =
  | ReadingStep
  | CompareStep
  | MultipleChoiceStep
  | FillBlankStep
  | SelectStep
  | OrderStep
  | MatchStep
  | CategorizeStep
  | TrueFalseStep
  | SentenceBuildStep
  | TranscribeStep
  | ErrorCorrectStep
  | ParagraphOrganizeStep
```

ID 규칙:
- 코스·유닛·레슨 ID는 비어 있지 않은 ASCII kebab-case다.
- 형식은 `^[A-Za-z0-9][A-Za-z0-9._:-]*$`, 길이 1~200.
- 의미를 드러내는 `course-...`, `unit-...`, `lesson-...` 접두사를 사용한다.
- 코스·유닛·레슨 ID는 계층을 통틀어 전역 중복이 없어야 한다.
- 스텝에는 `id`와 `sortOrder`를 쓰지 않는다.
- 각 스텝의 실제 ID는 배열 순서에 따라 `${lesson.id}-s${1부터 시작하는 순번}`이 된다.
- 선택지·구간·항목 ID는 화면 문구가 아니라 의미적으로 안정적인 비어 있지 않은 문자열이며, 해당 스텝 안에서 중복되지 않는다.

허용되는 `Step`은 아래 13개뿐이다. 없는 필드를 임의로 추가하지 않는다. `write`, `ai_feedback`, `copy`, `completion`, `annotate`, `summary`는 금지다. `illustrationAssetId`, `showNumbers`는 넣지 않는다.

```ts
type ReadingStep = {
  type: "reading"
  title: string
  guide: string
  body: string // CommonMark Markdown
  source?: string // 실제 확인한 규범·연구만. 자체 예문은 생략
}

type CompareStep = {
  type: "compare"
  title: string
  versions: Array<{
    label: string
    text: string
  }> // 최소 2개
  analysis: string
}

type MultipleChoiceStep = {
  type: "multiple_choice"
  question: string
  options: Array<{
    id: string
    text: string
  }> // 최소 2개, id 중복 금지
  correct: string // options[].id 중 하나
  explanation: string
  wrong?: string
}

type FillBlankStep = {
  type: "fill_blank"
  template: string // 빈칸은 정확히 "___"
  words: string[] // 최소 1개. 오답 토큰 허용
  wordIds: string[] // words와 같은 길이, 중복 금지
  answer: string[] // 중복 없는 wordIds의 부분집합. 길이 = template의 "___" 개수
  explanation: string
}

type SelectStep = {
  type: "select"
  question: string
  segments: string[] // 최소 1개
  segmentIds: string[] // segments와 같은 길이, 중복 금지
  correct: string[] // 중복 없는 segmentIds의 부분집합
  explanation: string
  layout?: "inline" | "block" // 이어진 문장은 inline, 독립 행은 block
}

type OrderStep = {
  type: "order"
  title: string
  items: string[] // 최소 1개
  itemIds: string[] // items와 같은 길이, 중복 금지
  correct: string[] // 모든 itemIds를 정확히 한 번씩
  explanation: string
}

type MatchStep = {
  type: "match"
  title: string
  guide: string
  pairs: Array<{
    left: string
    leftId: string
    right: string
    rightId: string
  }> // 최소 1개
  explanation: string
}

type CategorizeStep = {
  type: "categorize"
  title: string
  guide: string
  categories: Array<{
    id: string
    label: string
  }> // 최소 1개
  items: Array<{
    id: string
    text: string
    categoryId: string
  }> // 최소 1개
  explanation: string
}

type TrueFalseStep = {
  type: "true_false"
  statement: string
  correct: boolean
  explanation: string
  title?: string
}

type SentenceBuildStep = {
  type: "sentence_build"
  prompt: string
  tiles: Array<{
    id: string
    text: string
  }>
  correct: string[] // 정답 타일 ID의 순서 있는 배열
  explanation: string
}

type TranscribeStep = {
  type: "transcribe"
  source: string
  explanation: string
  title?: string
}

type ErrorCorrectStep = {
  type: "error_correct"
  segments: Array<{
    id: string
    text: string
  }>
  fixes: Array<{
    id: string
    text: string
  }>
  correctSegment: string
  correctFix: string
  explanation: string
  title?: string
}

type ParagraphOrganizeStep = {
  type: "paragraph_organize"
  cards: Array<{
    id: string
    text: string
  }>
  correct: string[] // 포함할 카드 ID의 순서 있는 부분집합
  explanation: string
  title?: string
}
```

추가 불변식:
- 모든 코스와 유닛에는 적어도 하나의 하위 항목이 있고, 모든 레슨에는 15~20개의 스텝이 있다.
- `time`은 `20분`처럼 양의 정수와 `분`만 사용한다.
- `fill_blank.template`의 `___` 개수와 `answer` 길이는 같다. 한 토큰은 한 칸에만 쓴다.
- `match`의 왼쪽 표시 텍스트, 오른쪽 표시 텍스트, `leftId`, `rightId`는 각 쪽에서 유일하다. 화면에서 구분할 수 없는 중복 문구를 두지 않는다.
- `categorize.items[].categoryId`는 존재하는 `categories[].id`를 참조한다. 카테고리 ID와 항목 ID에는 서로 다른 접두사를 사용한다.
- `order.correct`는 모든 `itemIds`의 완전 순열이다. 화면은 항목을 섞으므로 JSON 배열 순서에 의존하지 않는다.
- `sentence_build`의 한 타일은 한 번만 쓴다. `tiles`에 오답 타일을 둘 수 있다.
- `transcribe`의 정답은 `source`를 연속 공백 정규화해 대조한다. 창작 문장을 쓰게 하지 않는다.
- `error_correct`는 오류 구간 하나와 수정안 하나를 고른다. 수정안 문구는 서로 달라야 한다.
- `paragraph_organize`는 넣지 않을 카드(방해 문장)를 둘 수 있다. `order`와 달리 정답이 전 항목의 순열이 아닐 수 있다. 카드 문구는 유일해야 한다.
- `true_false`는 O·X만 있다. 선택지 배열을 두지 않는다. 두 해석이 동시에 성립하는 문장은 쓰지 않는다.
- 정답은 문구나 배열 위치가 아니라 안정적인 ID(또는 boolean, 또는 정규화한 원문)를 참조한다.
- 빈 문자열, 공백뿐인 문자열, 중복 선택지, 의미상 복수 정답인데 단일 정답만 허용한 문제를 만들지 않는다.
</repository_contract>

<construction_process>
파일은 내부 작업 공간에서 점진적으로 작성하라.

1. 전체 코스 카탈로그와 전역 ID 목록을 확정한다.
2. 첫 코스의 4주 목차를 확정한다.
3. 그 코스의 모든 레슨·스텝을 골드 스탠더드 밀도로 파일에 추가한다.
4. 해당 코스의 교육 품질, 출처, Markdown, 스키마를 검수한다.
5. 통과한 코스만 배열에 남기고 다음 코스로 간다.
6. 마지막에 전체 파일을 다시 파싱하고 전역 검증한다.

한 코스를 작성할 때:
1. 고유 학습 성과와 근거 묶음을 확인한다.
2. 제목, 설명, 카테고리, 가장 가까운 visualKey를 고른다.
3. 4주 유닛과 레슨의 점진적 흐름을 설계한다.
4. 각 레슨에서 목표를 관찰하기 좋은 메커니즘을 고른다. 주제로 타입을 고정하지 않는다.
5. 활동과 해설을 실제로 풀어 보고 정답의 결정성과 난이도를 확인한다.
6. 코스 안의 중복과 빠진 선수 설명을 제거한 뒤 다음 코스로 이동한다.
</construction_process>

<programmatic_validation>
최종 응답 전에 코드로 JSON을 파싱하고 다음을 모두 검사하라. 실패하면 파일을 수정하고 전부 다시 검사한다.

- JSON 파싱 성공과 루트 배열 길이 10
- 코스마다 유닛 4개, 레슨마다 스텝 15~20
- 모든 필수 필드의 타입과 허용 enum
- 코스·유닛·레슨의 비어 있지 않은 배열
- 전역 course/unit/lesson ID의 kebab-case, 길이, 문자 집합, 유일성
- 레슨 시간 형식과 양의 정수
- 허용된 13개 스텝 타입만 사용
- 각 스텝 타입의 필수·선택 필드와 추가 필드 부재
- 선택형 stable ID의 존재·유일성·참조 무결성
- ORDER 정답의 완전 순열
- MATCH 양쪽 텍스트·ID 유일성
- CATEGORIZE category 참조 무결성
- FILL_BLANK의 `___` 개수와 `answer` 길이
- TRUE_FALSE의 boolean 정답
- SENTENCE_BUILD 정답 ID가 tiles에 존재
- ERROR_CORRECT의 구간·수정안 참조
- PARAGRAPH_ORGANIZE 정답이 cards의 부분집합
- 빈 문자열, placeholder, TODO, 생략 기호
- 모든 READING.body의 비어 있지 않은 Markdown
- 모든 출처 URL의 실제 형식과 조사 중 연 자료와의 일치
- 제목·설명·요약·문항·해설의 완전성

검증이 끝난 뒤 JSON을 2칸 들여쓰기로 다시 직렬화하고 마지막 개행을 넣는다. 최종 파일에서 다시 파싱한 뒤 SHA-256과 계층별 개수를 계산한다.
</programmatic_validation>

<final_quality_audit>
기계 검증 뒤 다음 의미 감사를 수행하라. 하나라도 실패하면 해당 코스를 고친 뒤 구조 검증과 의미 감사를 다시 수행하라. 스키마만 맞고 품질이 프로토타입이면 제출하지 마라.

- 24개 수행 공간 중 실질적으로 비어 있는 칸이 없는가?
- 코스 제목만 다르고 같은 설명·활동을 반복하지 않는가?
- 아무 레슨 본문에서 제목만 가렸을 때 주제를 식별할 수 있는가?
- 같은 유닛의 두 레슨을 나란히 두면 예문·오개념·활동이 다른가?
- 공식 규범과 저자의 문체 취향을 혼동하지 않는가?
- 어린이·영어권 연구의 결과를 성인 한국어 효과로 단정하지 않는가?
- 각 핵심 원리의 정의, 절차, 예시, 연습, 피드백이 정렬되는가?
- 오답이 우스운 선택지가 아니라 흔한 오개념을 진단하는가?
- `reading`을 교과서 한 절로 읽어도 빈약하지 않은가? 골드 스탠더드보다 얇지 않은가?
- 확인 문항을 풀면 그 레슨의 핵심 구분이 실제로 연습되는가?
- 같은 스텝 타입을 주제 전용 칸에 가두지 않고, 목표에 맞게 여러 영역에 재사용했는가?
- 타입별 7칸 예시를 체크리스트로 채워 내용을 얕게 만들지 않았는가?
- 설명·설득·정보 판단에서 증거의 질과 윤리를 함께 다루는가?
- 매체·생성형 AI 환경의 최신 문제를 유행어가 아니라 검증 가능한 원리로 다루는가?
- 모든 코스가 선행 코스 없이 이해 가능한가?
</final_quality_audit>

<final_response>
완성된 `content-seed-data.json` 파일을 첨부하라. 본문은 다음 정보만 간단히 보고한다.

- 코스 / 유닛 / 레슨 / 스텝 수
- 파일 크기
- SHA-256
- JSON 파싱·스키마·참조 무결성·의미·품질 감사 통과 여부

연구 과정, 내부 추론, 코스별 설명과 JSON 본문을 대화 본문에 반복하지 않는다.
</final_response>
````

## 수령 후 후속 작업

이 파일의 범위가 아니다. 받은 JSON을 저장소에 넣기 전에 다음을 별도 작업으로 수행한다.

1. 보고된 SHA-256과 파일 해시를 비교한다.
2. 후보 파일을 별도 경로에 두고 JSON 파싱과 스키마 감사를 다시 실행한다.
3. `true_false`, `sentence_build`, `transcribe`, `error_correct`, `paragraph_organize`의 DTO, 답안, 시드 매퍼, 학습자 renderer, 관리자 폼을 맞춘다. 맞추기 전에는 권위 시드에 넣지 않는다.
4. 코스별 전수 검수를 한다. 규범, 출처 URL, 정답 결정성, 예문 중복, 저작권 위험을 본다.
5. 승인된 파일만 [`content-seed-data.json`](../../../packages/modules/content/src/infrastructure/persistence/content-seed-data.json)에 반영한다.
6. 시드 수량에 묶인 테스트를 새 기준선에 맞게 고친다.
